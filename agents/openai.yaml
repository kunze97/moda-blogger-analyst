interface:
  display_name: "Moda Blogger Analyst"
  short_description: "Analyze posts through the distilled investor-blogger framework."
  default_prompt: "Use $moda-blogger-analyst to interpret this blogger's latest post and explain the likely hidden implications."

policy:
  allow_implicit_invocation: true
